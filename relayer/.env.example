# RPC URL for the chain where the Gateway is deployed
RPC_URL="http://127.0.0.1:8545"

# Private key used by this relayer for signing and submitting txs (use test key only)
RELAYER_PRIVATE_KEY="0x0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"

# Destination gateway address (the gateway contract that will receive signed messages)
DEST_GATEWAY_ADDRESS=""

# Signature threshold expected by the destination contract
SIGNATURE_THRESHOLD=2

# Optional: an aggregator private key that will submit aggregated signatures
AGGREGATOR_PRIVATE_KEY=""
